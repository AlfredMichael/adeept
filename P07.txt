College: Technological University of Shannon 

Parchment 

#!/bin/bash
dir="./json_files"

# Concatenate all lines, wrap them in an array
jq -s '.' "$dir"/*.json > merged.json

jq -s -R -c 'split("\n") | map(select(length>0) | fromjson)' "%dir%\*.json" > merged.json