gst-launch-1.0 \
  filesrc location=your_raw12_file.raw ! \
  rawvideoparse format=bayer width=1920 height=1080 bpp=12 pattern=rggb ! \
  bayer2rgb ! \
  videoconvert ! \
  fpsdisplaysink video-sink=autovideosink text-overlay=true


gst-launch-1.0 \
  filesrc location=your_raw12_file.raw ! \
  rawvideoparse format=bayer width=1920 height=1080 bpp=12 pattern=rggb ! \
  bayer2rgb ! \
  videoconvert ! \
  autovideosink


import gi
gi.require_version('Gst', '1.0')
from gi.repository import Gst, GLib

def on_new_sample(sink, data):
    sample = sink.emit("pull-sample")
    buffer = sample.get_buffer()
    print("Got frame with size:", buffer.get_size())
    return Gst.FlowReturn.OK

Gst.init(None)

pipeline = Gst.parse_launch(
    "v4l2src ! videoconvert ! appsink name=sink emit-signals=true"
)

appsink = pipeline.get_by_name("sink")
appsink.connect("new-sample", on_new_sample, None)

pipeline.set_state(Gst.State.PLAYING)

loop = GLib.MainLoop()
loop.run()