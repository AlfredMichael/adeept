while inotifywait -e modify,create,delete /path/to/dir; do
    echo "Directory changed!"
done

- event driven but one shot

$watcher = New-Object IO.FileSystemWatcher "C:\path"
$watcher.EnableRaisingEvents = $true

Register-ObjectEvent $watcher Created -Action { Write-Host "Created!" }

while ($true) {
    Wait-Event | Out-Null
}

- event driven